mod class_model;
mod room_model;
mod schedule_model;
mod student_model;

pub use class_model::{Class, ClassId};
pub use room_model::{Room, RoomId};
pub use schedule_model::{Schedule, ScheduleId};
pub use student_model::{Student, StudentId};
